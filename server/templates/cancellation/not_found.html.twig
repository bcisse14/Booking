{% extends 'base.html.twig' %}

{% block title %}Lien d'annulation invalide{% endblock %}

{% block stylesheets %}
<style>
    body {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Arial, sans-serif;
        color: #2d3748;
    }
    .container {
        max-width: 720px;
        margin: 48px auto;
        background: #fff;
        border-radius: 12px;
        padding: 32px;
        box-shadow: 0 20px 60px rgba(0,0,0,0.15);
    }
    .header {
        text-align: center;
        padding: 24px 0;
    }
    .header h1 { font-size: 28px; margin-bottom: 8px; }
    .message { margin-top: 18px; background:#fff3f2; border:1px solid #fed7d7; padding:16px; border-radius:8px; }
    .actions { text-align:center; margin-top:20px; }
    .btn { display:inline-block; padding:10px 20px; border-radius:8px; color:#fff; text-decoration:none; font-weight:600; }
    .btn-home { background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); }
</style>
{% endblock %}

{% block body %}
<div class="container">
    <div class="header">
        <div style="font-size:56px;">⚠️</div>
        <h1>Lien d'annulation invalide</h1>
        <p>Le lien que vous avez suivi est invalide ou a déjà été utilisé.</p>
    </div>

    <div class="message">
        <strong>Informations :</strong>
        <p>Si vous pensez que ceci est une erreur, contactez l'administrateur ou essayez de retrouver votre email de confirmation pour obtenir le bon lien.</p>
    </div>

    <div class="actions">
        <a class="btn btn-home" href="{{ app.request.schemeAndHttpHost }}">← Retour à l'accueil</a>
    </div>
</div>
{% endblock %}
